# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files — never commit API keys
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# raw source media (WhatsApp originals) — kept local, not needed in repo
/_source-originals/

# patient before/after photos — held back pending consent (this is a public repo)
/public/clinic/transformations/
